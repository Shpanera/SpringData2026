# spatial_lib

Библиотека для работы с городскими пространственными данными.

## Установка

```bash
pip install spatial_lib
```

## Пример

```
from spatial_lib import calculate_area

coords = [(0,0), (1,0), (1,1), (0,1)]
print(calculate_area(coords)) 
```
